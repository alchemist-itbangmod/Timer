import React from 'react';
import { compose,
         withState,
         withHandlers,
         lifecycle } from 'recompose';

const TimePage = props => (
  <div>
    TimePage
  </div>
)

export default TimePage